import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        //Main, grey-ish color
        main: '#373737',
      },
      secondary: {
        main: '#dc004e',
      },
    },
  });

  export default theme;